{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Lua (WH3)",
      "type": "lua",
      "request": "launch",
      "program": "${workspaceFolder}/wh3_lua_debugger/vscode/debug.lua",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Publish Lua Snippet",
      "stopOnEntry": false,
      "env": {
        "DEBUG": "${file}"
      }
    }
  ]
}
